apiVersion: v1
kind: Service
metadata:
    name: mople-dev-svc
    namespace: mople
spec:
    type: ClusterIp
    selector:
        app: mople-dev
    ports:
        - protocol: TCP
          port: 8080
          targetPort: 8080